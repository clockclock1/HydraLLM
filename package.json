{
  "name": "hydrallm",
  "version": "0.1.0",
  "private": false,
  "description": "OpenAI-compatible LLM failover proxy with a visual management UI.",
  "type": "module",
  "bin": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build:ui": "npm --prefix ui run build && node scripts/copy-ui.mjs",
    "check": "node --check server.js && npm --prefix ui run build"
  },
  "engines": {
    "node": ">=20"
  },
  "pkg": {
    "assets": [
      "public/**/*"
    ],
    "outputPath": "dist",
    "bytecode": false
  },
  "devDependencies": {
    "@yao-pkg/pkg": "^6.21.0"
  }
}
