{
  "name": "hydrallm",
  "version": "0.1.0",
  "private": false,
  "description": "OpenAI-compatible LLM failover proxy with a visual management UI.",
  "type": "module",
  "scripts": {
    "dev:ui": "npm --prefix ui run dev",
    "build:ui": "npm --prefix ui run build && node scripts/copy-ui.mjs",
    "check": "npm --prefix ui run build"
  },
  "engines": {
    "node": ">=20"
  }
}
